# Revisions that only moved code around. `git blame` should skip them so it
# keeps pointing at whoever last changed the logic.
#
# Enable locally (once per clone):
#   git config blame.ignoreRevsFile .git-blame-ignore-revs
#
# GitHub applies this file automatically.

# Format with the ESLint presets instead of a hand-tuned style
e59a46f4bb2f3eabe3e7633f7304377bc20fc6c5
