{
  "name": "sillytavern-claude-oauth",
  "version": "0.1.0",
  "private": true,
  "description": "SillyTavern server plugin: Claude Pro/Max subscription login (OAuth) for the built-in Claude chat completion source.",
  "type": "module",
  "main": "index.mjs",
  "scripts": {
    "test": "node test/smoke.mjs",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "vendor": "node scripts/build-vendor.mjs",
    "vendor:check": "node scripts/build-vendor.mjs --check",
    "check": "eslint . && tsc --noEmit && node scripts/build-vendor.mjs --check && node test/smoke.mjs"
  },
  "packageManager": "pnpm@12.3.4",
  "//": "No runtime dependencies on purpose: the Claude OAuth flow is vendored into vendor/ by scripts/build-vendor.mjs. Everything below is dev-only.",
  "dependencies": {},
  "devDependencies": {
    "@earendil-works/pi-ai": "0.86.1",
    "@eslint/js": "^10.0.1",
    "@stylistic/eslint-plugin": "^5.10.0",
    "@types/node": "^26.6.2",
    "esbuild": "0.28.2",
    "eslint": "^10.11.0",
    "globals": "^17.12.0",
    "typescript": "^7.0.2"
  },
  "engines": {
    "node": ">=20.6"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Xerxes-2/sillytavern-claude-oauth"
  },
  "license": "MIT"
}
